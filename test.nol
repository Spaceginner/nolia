fnc println |n: %i 64|
asm {
    syscall println;
}

fnc add |a: %i 64, b: %i 64| -> %i 64
asm {
    syscall add;
}

fnc funny |check: %bool| -> %i 64 {
    #if check {
        return 5;
    };
    
    6
}

fnc entry || {
    println(funny(false));  // 6
    println(funny(true));  // 5
}
